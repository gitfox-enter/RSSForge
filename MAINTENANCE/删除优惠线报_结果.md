# 删除 优惠线报 feed — 结果

**时间**：2026-07-19 07:51 | **提交**：`27279477` | **状态**：已推送，线上已生效

## 删除项

| feed 文件 | 源名称 | 源 URL | 线上状态码 |
|---|---|---|---|
| `you-hui-xian-bao.xml` | 优惠线报 | https://www.yxssp.com/ | 404 ✅ |

## 同步改动

- **`sites.yaml`**：移除「优惠线报」源配置（否则 CI 下次运行会重新生成该 feed）。`sites` 总数 `24 → 23`。
- **`docs/index.html`** + **4 个 OPML**（`opml.xml` / `opml.official.xml` / `opml.ghfast.xml` / `opml.jsdelivr.xml`）：重生成，均 `24 → 23`。
- **`docs/version.txt`**：由生成脚本更新为 `feeds: 23`。

## 线上验证（GitHub Pages 已重建）

| 校验项 | 结果 |
|---|---|
| 4 个 OPML outline 数 | 23 / 23 / 23 / 23 ✅ |
| `you-hui-xian-bao.xml` | 404 ✅ |
| 首页 index.html 列出 feed | 23 ✅ |

## 本次关键坑（已修复）

`git merge -X ours` 对「文件删除 vs 远端修改」冲突会保留远端（CI）的修改版。
第一次推送时它把已删除的 `you-hui-xian-bao.xml` 又合并回来 → 线上仍是 24。
修复：合并后**强制再删该文件并重生成**，第二次推送一次成功，CI 已跟随 23 源配置。

## 备份

`/root/.codebuddy/artifact/backup_del_youhui_20260719_0748/you-hui-xian-bao.xml`
